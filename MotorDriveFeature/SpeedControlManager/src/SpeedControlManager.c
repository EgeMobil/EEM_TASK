// baslangic yorumu doxy'gene uygun sekilde yapilacak

// include private methodlarin implemantasyonlariinin yapilacagi yer burasi

// instance static global tanimi burada yapilacak

// tum getter/setter'lar dogru tipleriyle tanimlanacak _Impl suffix'i ile instance kullancak

// toString icin retVal ve enum kullanan bir attribute varsa, get char* 
// helper fonksiyonlar tanimlanacak

// toString methodu formata uygun tasarlanacak
/*
    "[KomponentAdı]: Interface baglanti durumlari, attribute durumlari(enum olanlar getHelper ile)"
*/

// CTOR'un implementasyonu yapilacak

// Instance döndüren fonksiyon yapilacak

// Ekstra fonksiyon varsa onlarin implementasyonu yapilacak( tasarimina bagli. definition'da detaylar var.)
